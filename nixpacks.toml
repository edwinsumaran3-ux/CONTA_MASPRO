[phases.setup]
nixPkgs = ["python311", "gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install --no-cache-dir ."]

[phases.build]
cmds = []

[start]
cmd = "alembic upgrade head && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
